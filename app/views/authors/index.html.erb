<h1>Index of Authors</h1>

<ul>
  <% @authors.each do |author| %>
    <li>
      <%= link_to "#{author.name} #{author.surname} - #{author.age}", author_path(id: author.id) %>
    </li>
  <% end %>
</ul>